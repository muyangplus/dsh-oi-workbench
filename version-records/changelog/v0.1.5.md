# v0.1.5 — 发布版 README 修正 + 剩余规划能力（一键整场 / spj·交互验证 / story 集成）

> 承接已发布到 npm 的 `@muyangplus/dsh-oi-workbench@0.1.4`。0.1.4 的发布包内 README
> 误写“安装时自动安装 dsh-commands”，本版修正为“可选项，已安装才自动注册”；
> 同时补齐剩余规划能力。按用户约定，0.1.5 不再手动 `npm publish`，改由 GitHub 推送后 CI 自动发布。

## Fixed

- 发布版 `public/README.md`：斜杠命令表述与行为对齐——`@etby-studio/dsh-commands` 为**可选项**，
  未安装则斜杠命令不可用；安装后补丁才扫描 `commands/register/` 自动注册。
- `public/package.json` 与根一致：`dependencies` → `peerDependencies`（`optional: true`）。

## Added

- `generator/generate_contest.py`：从比赛 manifest 一键生成整场比赛
  （自动应用 story-card → 生成并检查 `story.yaml` → 调用 `build_contest.py` 产出
  完整样例 + 整卷 PDF + Lemon(.cdf) + 每题 Hydro/HOJ 包）。
- `generator/validate_spj.py`：spj/交互题样例验证自动化
  （标程应 100/100；故意错误程序应被 checker/interactor 击杀，不能 100）。
- `story_card.py` 幂等标记 `<!-- story-card -->`：重复应用不重复插入故事段。

## Changed

- 版本号升至 **0.1.5**；包名保持 `@muyangplus/dsh-oi-workbench`。

## 验证（本机）

- `generate_contest.py` 在临时目录跑通：story.yaml 生成 + 连续性检查 + 全套交付物；
- `validate_spj.py` 对 checker 与 interactor 均通过（std 100/100，bad 0/100）；
- `blind_review.py --release`：0 问题 0 提示；
- 全部新增脚本 `py_compile` 通过。
