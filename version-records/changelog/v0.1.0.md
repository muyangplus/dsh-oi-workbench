# v0.1.0 — dev 仓库基线

> dev 线（`muyangplus/dsh-oi-workbench-dev`，Apache-2.0）重启版本后的起始版本；
> 正式启用 `version-records/` 版本记录体系前的能力快照。
> 本条目为「既有能力快照」，非本次新增。

## Added（既有能力，基线快照）

- skill-first 插件：`oi-workbench` 技能，覆盖 知识点锁定 → 测试点/分数表设计 → 代码矩阵 →
  数据构造与击杀矩阵验证 → 本地评测 → Hydro/HOJ 打包 → 发布管理 全流程。
- 内置知识库 `knowledge-base/`：`level-1-basic.md` / `level-2-intermediate.md` /
  `level-3-expert.md` + `workflow.md`（知识点锁定工作流）。
- 原创参考题 `reference/`：`entry/`（入门级 4 题）+ `intermediate/`（提高级 4 题），
  每题含 problem.md / spec.json / VERIFICATION.md / data / sample / std / brute / generator。
- 打包器与本地评测 `generator/`：build_package / build_hoj_package / verify_* / local_judge / build_all_reference。
- 双 OJ 桥 `hydro-bridge/` 与 `hoj-bridge/`；Python 外部管理 UI `ui/`；
  系统斜杠命令 `/oiwb` `/oj` `/hoj`（经 `@etby-studio/dsh-commands` 自动注册）。

## Changed

- 启用 Apache-2.0；
- 知识库重构为「入门/提高/专家」级别（level-1/2/3）；
- 参考题重构为 entry / intermediate 两级。

## Fixed

- （基线，无）

## 说明

- 本版本是 dev 线重启后的起始版本；public 线既往版本记录见 public 仓库（`muyangplus/dsh-oi-workbench`）历史。
