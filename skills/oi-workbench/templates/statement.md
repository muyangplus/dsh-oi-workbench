# {{题目名}}

## 题目描述

{{背景 1-2 句（可选）}}

给定{{对象}}，{{任务目标，必须在此段可读懂}}。

## 输入格式

{{文件读写时：从文件 {{文件名}}.in 读取，输出到 {{文件名}}.out（文件名一律英文小写，见 spec.io）。}}
第一行输入一个整数 $n$（$1\le n\le {{上限}}$）。

接下来 $n$ 行，每行输入两个整数 $x_i, y_i$（${{下界}}\le x_i, y_i\le {{上限}}$）。

{{多测时：第一行输入一个整数 $T$（$1\le T\le {{上限}}$），表示数据组数。}}

## 输出格式

{{输出内容描述，每行几个数，是否取模}}

## 样例输入 1

```
{{小样例}}
```

## 样例输出 1

```
{{小样例}}
```

## 样例输入 2

```
{{小样例}}
```

## 样例输出 2

```
{{小样例}}
```

## 样例解释 1

{{可选；不得泄露关键结论/核心做法}}

## 样例输入 3

（见题目附件 [{{样例文件}}.zip](file://{{样例文件}}.zip)，该样例满足测试点 {{区间}} 的约束。）

## 数据范围

对于所有测试数据，保证：

- $1\le {{n}}\le {{上限}}$；
- {{每个变量的总体保证}}。

| 测试点 | {{变量1}} | {{变量2}} | 特殊性质 |
|---|---|---|---|
| $1,2$ | $\le {{10}}$ | $\le n$ | 无 |
| $3\sim 5$ | $\le {{18}}$ | $\le n$ | 无 |
| $6\sim 8$ | $\le {{10^2}}$ | $\le n$ | A |
| ... | ... | ... | ... |
| $22\sim 25$ | $\le {{上限}}$ | $\le n$ | 无 |

特殊性质 A：{{定义}}。

时间限制：1s；空间限制：{{512}}MB。

{{比赛封面/配置文件补充：
编译选项（C++）：`-O2 -std=c++14 -static`
全国统一评测机器配置：Intel Core Ultra 9 285K CPU @ 3.70 GHz（关闭睿频与能效核），内存 96 GB。
}}
